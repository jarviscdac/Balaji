#!/bin/bash


echo "Enter the file name: "
read file_name


if [ ! -f "$file_name" ]; then
  echo "File not found!"
  exit 1
fi


if [ -r "$file_name" ]; then
  echo "The file '$file_name' is readable by the user."
else
  echo "The file '$file_name' is not readable by the user."
fi